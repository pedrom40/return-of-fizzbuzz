'use strict';

/*
  REQUIREMENTS

  1. User submits form with count number
  2. stop default behavior
  3. for each count, append a new div like this:
      <div class="fizz-buzz-item">
        <span>1</span>
      </div>
  4. If current number is divisible by 3 add .fizz class to div
  5. If current number is divisible by 5 add .buzzz class to div
  6. If current number is divisible by 5 and 3 add .fizzbuzz class to div

*/

function handleFormSubmission () {

  console.log('hello console');

}

$(handleFormSubmission)
